enum GLOBAL {
    UNIT_LENGTH = 100,
    TICK_PER_SEC = 20,
    TICK_PERIOD = 50,
    // Must be an odd number in order to make it bilaterally symmetrical
    VIEWPORT_COLUMNS = 7,
    VIEWPORT_ROWS = 7,
    // Must be an odd number in order to make it bilaterally symmetrical
    RENDER_COLUMNS = 9,
    RENDER_ROWS = 9
}

export default GLOBAL;
