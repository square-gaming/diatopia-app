export enum GLOBAL {
    UNIT_LENGTH = 100,
    // Must be an odd number in order to make it bilaterally symmetrical
    VIEWPORT_COLUMNS = 7,
    VIEWPORT_ROWS = 7,
    // Must be an odd number in order to make it bilaterally symmetrical
    RENDER_COLUMNS = 9,
    RENDER_ROWS = 9
}